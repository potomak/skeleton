%h2
  Account informations
%p
  %b
    Email:
  = @user.email
  %br
  %b
    Login count:
  = @user.login_count
  %br
  %b
    Last request at:
  = @user.last_request_at
  %br
  %b
    Last login at:
  = @user.last_login_at
  %br
  %b
    Current login at:
  = @user.current_login_at
  %br
  %b
    Last login ip:
  = @user.last_login_ip
  %br
  %b
    Current login ip:
  = @user.current_login_ip

%p
  != link_to 'Edit', edit_account_path
  %br
  != link_to_remote 'Delete', :url => {:controller => "users", :action => "destroy", :id => @user.id}, :method => :delete, :confirm => "Are you sure?"